package Controllers;

import java.awt.event.MouseEvent;

public class DescController {

  /**
   * Take in list of circles, get x and y coordinates then compare with user click location from
   * mousepress event
   *
   * <p>https://stackoverflow.com/questions/27999430/javafx-clickable-line-on-canvas
   *
   * @param mouseEvent and array of circles
   */
  public void getMouseClick(MouseEvent t) {

    // go through all circles

    // check with current mouse press data

    // etc.
    int x = 0;
    int y = 0;
    displayDescription(x, y); // z
  }

  public void displayDescription(int x, int y) {}
}
